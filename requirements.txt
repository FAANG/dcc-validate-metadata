celery==5.3.1
python-decouple==3.1
# SECURITY: Django 3.2 is end-of-life (extended support ended 2024-04). 3.2.25
# is the final 3.2 release with all available security fixes. Migrate to a
# supported LTS (4.2+) - tracked separately as it needs integration testing.
Django==3.2.25
channels==3.0.3
django-cors-headers==3.5.0
# SECURITY: outdated - upgrade after testing. requests>=2.32 (CVE-2023-32681,
# CVE-2024-35195), aiohttp>=3.10 (CVE-2024-23334 path traversal, smuggling).
requests==2.24.0
xlrd==1.2.0
aiohttp==3.7.4
lxml==4.9.1
xlsxwriter==1.3.7
redis==3.5.3
channels-redis==3.4.1
elasticsearch==7.16.2
pandas==2.0.3
psycopg2-binary
djangorestframework==3.12.2
# SECURITY: djangorestframework-jwt is unmaintained (since ~2018). Migrate to
# djangorestframework-simplejwt - tracked separately (code changes required).
djangorestframework-jwt==1.11.0
drf-yasg==1.20.0
flower==2.0.0
uvicorn==0.14.0
websockets==10.3
graphql-ws==0.4.4
graphene-django==2.15.0
deepdiff==6.2.3